package main

import "fmt"

func main() {

	// para asignarle valor a una variable, se especifica una variable con el tipo de dato int
	// Luego le asignamos un valor en el main
	var valor int

	// Declaración de variables cortas
	// test := 25 (la declaración corta solo debe usarse en funciones)

	// bloque de declaración de variabes
	var (
		product  = "curso"
		cantidad = 25
		price    = 25.40
		inStock  = true
	)
	valor = 20
	fmt.Println(valor)
}
